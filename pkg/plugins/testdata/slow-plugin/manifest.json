{
  "protocol_version": 1,
  "name": "slow",
  "version": "0.1.0",
  "description": "Source plugin whose reads block, used by the cancellation tests",
  "binary": "./bin",
  "node_types": [
    {
      "type": "source_slow",
      "kind": "source",
      "display_name": "Slow Source",
      "description": "Blocks during read so the host's cancellation path can be exercised"
    }
  ]
}
